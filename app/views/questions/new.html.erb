<%= form_for @question, html: { id: 'question-form' } do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :description %>
  <%= f.text_area :description %>

  <%= f.submit 'Ask!' %>
<% end %>
